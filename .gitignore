# node
node_modules/
npm-debug.log*

# build artifact — published via GitHub Releases, not tracked in source
main.js
*.js.map

# editors
.DS_Store
.vscode/
.idea/

# obsidian local install symlink (if dogfooding via ln -s into vault)
test-vault/

# The repo is its own Obsidian vault (see CLAUDE.md § Dogfooding). Its committed
# config is the bare minimum; everything Obsidian writes per-machine stays out.
.obsidian/plugins/
.obsidian/workspace*
.obsidian/appearance.json
.obsidian/hotkeys.json
.obsidian/core-plugins*.json
